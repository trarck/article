靠谱的有这些：
1、GenyMotion（依赖于VirtualBox）
2、qemu（门槛比较高）
3、直接上VirtualBox装个remixos总的来说，
3的兼容性最好，因为既支持x86应用也支持arm应用，但资源开销比较庞大，想流畅运行建议分配2G以上RAM。
2说白了其实是在x86或amd64下模拟出一个arm环境，所以使用门槛比较高。
而1只能运行集成x86库的程序，所以。。。像QQ这样只有arm库的程序是无法运行的，但假如没有运行这类应用的需求，GenyMotion还是很不错的，是一个折衷的选择。
说了这么多，为什么不去装个ChromeOS？
